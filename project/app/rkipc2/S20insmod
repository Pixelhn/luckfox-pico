ko_init()
{
	default_ko_dir=/oem/usr/ko
	if [ -f "$default_ko_dir/insmod_ko.sh" ];then
		cd $default_ko_dir && sh insmod_ko.sh && cd -
	fi
}

case $1 in
        start)
	        ko_init
                ;;
        *)
                exit 1
                ;;
esac